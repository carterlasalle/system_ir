# Next.js Full-Stack

This repository is a Next.js application with App Router API routes that
serve transcripts, a library layer holding the transcript store, and React
components that render transcripts on the client. The API routes are the
contract boundary; the library owns persistence.

## Fixture notes

Golden repository for SCC tests: Next.js route handlers and full-stack
data flow (SCC-003 Next.js category).
