# Monorepo Acceptance Fixture

This repository contains a frontend, an API, a background worker, a shared
queue and database, and an external model service. The API serves transcript
records; the frontend and the worker both consume the API response contract.

## Fixture notes

Used by the acceptance scenario and context precision/recall tests.
