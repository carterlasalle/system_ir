# Users API + Web

This repository contains an express API that serves user records through a
database client, and a small web view that renders the user list fetched from
the API. The API owns the users data; the web layer only reads it.

## Fixture notes

Golden repository for SCC tests: frontend/API contract split with shared
database ownership.
