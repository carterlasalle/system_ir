[work."WORK-SCC-001"]
title = "System Context Compiler 10/10 plan execution (waves 1-9)"

[work."WORK-SCC-001".mirrors]
github_issue = "1"

[work."WORK-SCC-002"]
title = "External validation: real-repo corpus, ground truth, agent benchmark"

[work."WORK-SCC-003"]
title = "Benchmark v2: structured ontology, precision, density, gap diagnosis"

[work."WORK-SCC-004"]
title = "Semantic fact layer + framework semantics across languages"

[work."WORK-SCC-005"]
title = "Repo ontology: archetypes, hierarchy, state authority, CFG causality"

[work."WORK-SCC-006"]
title = "Holdout generalization wave"

[work."WORK-SCC-013"]
type = "work"
title = "Wave 13: occurrence identity, blind lock, package!=atom clustering"

[work."WORK-SCC-014"]
title = "Wave 14: full context stack (surface map, ranker, structural source, startup fusion)"

[work."WORK-SCC-015"]
title = "Wave 15.1: pipeline unification (one build_surface service, Claude fused startup, rank edges, honest ablations)"
[work."WORK-rank-edge-ontology-c-o-n-t-a-i-n-s-p-a-r-t-i-c-i-p-a-t-e-s-i-n-h-a-n-d-l-e-s-d-e-f-i-n-e-s-enter-the-p-p-r-adjacency"]
title = "Rank-edge ontology: CONTAINS/PARTICIPATES_IN/HANDLES/DEFINES enter the PPR adjacency"
status = "active"
origin.source = "behavior-change"
origin.intent = "Heterogeneous rankable nodes exist but membership/participation/invocation/definition predicates never enter the PageRank adjacency, leaving components/flows/routes/schemas disconnected from the rank graph. Add an explicit RankEdgeKind ontology (distinct from reference_kind) so CONTAINS (container->member 0.4, member->container ranking transition 0.8), PARTICIPATES_IN (symbol->flow 0.6, flow->participant 1.2), HANDLES (symbol->route 1.5), and DEFINES (symbol->schema 1.2) feed PPR with deliberate direction and weight."
origin.session = "default"
[work."WORK-wave-15-1-one-authoritative-surface-service-build-surface"]
title = "Wave 15.1: one authoritative Surface service (build_surface)"
status = "active"
origin.source = "behavior-change"
origin.intent = "Unify every surface consumer (production, CLI, MCP, plugin, benchmark ablations, startup) on ONE authoritative surface pipeline: build_surface(Global|Task) with token-aware quotas, soft budget + hard max with structural compression, and build_surface_staged ablation toggles; route startup.rs through build_surface and delete its duplicate task/PPR selection implementations."
origin.session = "default"
[work."WORK-implement-the-one-authoritative-surface-service-in-scc-context-build-su"]
title = "Implement the one authoritative Surface service in scc-context: build_su\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Implement the one authoritative Surface service in scc-context: build_surface(Global|Task) with token-aware quotas, soft budget + hard max with structural compression, build_surface_staged ablation toggles, route startup through build_surface, delete duplicate task/PPR selection in startup.rs"
origin.session = "default"
[work."WORK-wave-15-2-heterogeneous-hierarchy-edges-semantic-scoring-explain-rank-caching"]
title = "Wave 15.2 \u2014 heterogeneous hierarchy edges, semantic scoring, explain, rank caching"
status = "active"
origin.source = "new_feature"
origin.intent = "Complete the reviewer's 7 remaining items: full containment hierarchy in the rank graph (System-Subsystem-Service-Component-File-Symbol), HandledBy/DefinedBy ranking transitions, live semantic 10% in final importance, real --explain score decomposition, per-ModelEpoch rank caching with single-pass startup, hard-max invariant on rendered text, adaptive startup budgets."
origin.session = "default"
[work."WORK-trace-layer-adapter-for-system-ir"]
title = "TraceLayer adapter for system_ir"
status = "active"
origin.source = "new_feature"
origin.intent = "Implement TraceLayer as an evidence adapter for system_ir, parsing trace:v1 markers from source files and emitting structured facts in System IR (requirements, work items, implementation links, test verification links, decision/ADR links)."
origin.session = "default"
[work."WORK-implement-trace-layer-as-an-evidence-adapter-for-system-ir-parsing-trace"]
title = "Implement TraceLayer as an evidence adapter for system_ir, parsing trace\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Implement TraceLayer as an evidence adapter for system_ir, parsing trace:v1 markers from source files and emitting structured facts in System IR"
origin.session = "default"
[work."WORK-create-integration-tests-for-the-trace-layer-adapter"]
title = "Create integration tests for the TraceLayer adapter"
status = "active"
origin.source = "new_feature"
origin.intent = "Create integration tests for the TraceLayer adapter"
origin.session = "default"
[work."WORK-rank-cache-profiling-evidence-for-surface-pipeline"]
title = "Rank-cache profiling evidence for surface pipeline"
status = "active"
origin.source = "maintenance"
origin.intent = "Measure global-PPR rebuild cost vs the per-ModelEpoch GlobalRankCache seam so the cache-wiring decision for plain `scc surface` is empirical (spec Part F: profile before optimizing; if negligible, document and leave)."
origin.session = "default"
[work."WORK-task-context-transport-parity"]
title = "Task context transport parity"
status = "active"
origin.source = "behavior_change"
origin.intent = "One complete task artifact (pack + surface delta) derived from a single builder consumed by CLI text, CLI JSON, MCP, HTTP, Hermes and both SDKs, so transport choice cannot change semantic quality (spec Part C)."
origin.session = "default"
[work."WORK-write-changelog-for-v0-1-0-release-of-system-context-compiler"]
title = "Write changelog for v0.1.0 release of System Context Compiler"
status = "active"
origin.source = "new_feature"
origin.intent = "Write changelog for v0.1.0 release of System Context Compiler"
origin.session = "default"
[work."WORK-v0-1-0-release-changelog"]
title = "v0.1.0 Release Changelog"
status = "active"
origin.source = "non_behavioral_edit"
origin.intent = "Document the first public release of SCC with feature summary, SDK descriptions, and fix list"
origin.session = "default"
[work."WORK-benchmark-infrastructure-regression-suite-ledger-visibility-incremental-freshness"]
title = "Benchmark infrastructure regression suite: ledger visibility + incremental freshness"
status = "active"
origin.source = "new_feature"
origin.intent = "Add CI regression tests proving the Context Ledger records only model-visible entries (SCC mission 53) and that incremental path refresh serves fresh context after edits/deletions (54)"
origin.session = "default"
[work."WORK-mission-final-report-document"]
title = "Mission final report document"
status = "active"
origin.source = "non_behavioral_edit"
origin.intent = "Write docs/MISSION_FINAL_REPORT.md capturing the completed SCC hardening/verification/benchmark mission results (report only; no behavior)"
[work."WORK-p0-omp-integration-correctness-and-writable-benchmark-scientific-validit"]
title = "P0 OMP integration correctness and writable-benchmark scientific validit\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "P0 OMP integration correctness and writable-benchmark scientific validity: post-edit reindex --paths + loud index failure; session.compacting checkpoint restore; AGENTS.md shadowing and marker-aware rewrite; SCC_BIN; opaque mutation refresh; MCP/docs/skill/CI; TraceLayer OMP package + session_stop; TaskContext truncate_to hard cap; behavioral evaluators replacing comment-gamable greps; true scc-full with Structural Source, shared token cap, one writable runner, metadata, aggregation, paired CIs, invalid marking of old results."
origin.session = "default"
[work."WORK-ripwire-lessons-phase1"]
title = "Ripwire lessons Phase 1: extraction truth, resolver honesty, handles, language SoT"
status = "active"
origin.source = "new_feature"
origin.intent = "Absorb Ripwire-level extraction fidelity (receiver-aware resolution, reference roles, unresolved≠external, analysis-quality gauges, stable content handles, exact-source dominance, generated language matrix) without collapsing SCC's System Atlas / Reality Graph / provenance model into a call graph."
origin.session = "default"
[work."WORK-ripwire-lessons-phase2"]
title = "Ripwire lessons Phase 2: retrieval lenses (BM25, query routing, ablations)"
status = "active"
origin.source = "new_feature"
origin.intent = "Add a relevance-first retrieval lens (subtoken BM25, query-shape routing, exact anchors) that stays inspectable and off the production fused ranker until ablations justify a change. Importance (PPR) and relevance (lexical) must not be silently mixed."
origin.session = "default"
[work."WORK-ripwire-lessons-phase3"]
title = "Ripwire lessons Phase 3: actionable task packs, tests_to_run, budget rollover"
status = "active"
origin.source = "new_feature"
origin.intent = "Give Task Context a tests_to_run list with explicit reasons and a fixed-percent budget allocator with unused-quota rollover, without collapsing SCC levels or switching the production adaptive packer until ablation evidence exists."
origin.session = "default"
[work."WORK-ripwire-lessons-phase4"]
title = "Ripwire lessons Phase 4: honest scan classification for registry languages"
status = "active"
origin.source = "new_feature"
origin.intent = "Classify IndexSearch registry languages at scan time without claiming extractors or collapsing SCC's semantic walkers into .scm queries."
origin.session = "default"
[work."WORK-ripwire-lessons-phase5"]
title = "Ripwire lessons Phase 5: agent-loop vs baseline and Ripwire"
status = "active"
origin.source = "new_feature"
origin.intent = "Measure Task Context localization against a lexical baseline and black-box Ripwire pack-task with clustered stats, without injecting gold or adding MCP tools."
origin.session = "default"
[work."WORK-ripwire-lessons-phase6"]
title = "Ripwire lessons Phase 6: state authority, observed upgrades, forgotten partners, rpc bridges"
status = "active"
origin.source = "new_feature"
origin.intent = "Feed per-function store R/W into State Authority without replacing ownership, upgrade matching CALLS with OBSERVED_AS without rewriting EXTRACTED history, disclose co-change forgotten partners on impact, and link protobuf rpc contracts as INVOKES/IMPLEMENTS rather than cross-language CALLS."
origin.session = "default"
[work."WORK-phase-7-of-scc-x-ripwire-lessons-1-one-hop-type-narrowing-from-unique"]
title = "Phase 7 of SCC x Ripwire lessons: (1) one-hop type narrowing from unique\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 7 of SCC x Ripwire lessons: (1) one-hop type narrowing from unique local constructor and parameter type bindings so x.process() pins to Order.process when x = Order() uniquely, never spraying same-name methods and never inventing candidates; (2) a real three-way JSONL exploration protocol in scc bench loop --explore where a deterministic pack-consumer agent emits search/read/graph tool events for baseline vs SCC vs Ripwire using the same metrics as bench agent (files opened, search calls, first-correct, substitution), with AGENT_CMD as a later LLM plug-in; (3) watcher-fail falls back to content-hash sweep. Do not change production ranking, do not add a fifth context level, do not add MCP tools, do not rewrite REQ-resolution-honesty-gauges."
origin.session = "default"
[work."WORK-phase-8-of-scc-x-ripwire-lessons-one-hop-field-type-narrowing-unique-c"]
title = "Phase 8 of SCC x Ripwire lessons: one-hop field-type narrowing. Unique c\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 8 of SCC x Ripwire lessons: one-hop field-type narrowing. Unique class field types from Python self.x = Order() / class x: Order / param assigned to self.x, and TypeScript class fields / this.x = new Foo(), pin self.x.m() and this.x.m() (exactly one field hop) to Type.m when that method exists locally or on the imported type. Two types tombstone. Longer chains stay unresolved. No same-name spray. EXTRACTED provenance. Binds are extract-time only. Do not change production ranking, MCP tool count, or context levels."
origin.session = "default"
[work."WORK-phase-9-of-scc-x-ripwire-lessons-java-one-hop-field-type-narrowing-uni"]
title = "Phase 9 of SCC x Ripwire lessons: Java one-hop field-type narrowing. Uni\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 9 of SCC x Ripwire lessons: Java one-hop field-type narrowing. Unique Java class field types pin this.field.method() the same way Python and TypeScript already pin self/this field methods. The Java extractor must record class-scoped TypeBinds from field declarations (private Order repo), this.x = new Foo(), and this.x = param when the param has a unique type in that constructor or method. Two distinct types for the same field tombstone and must not narrow. Longer chains this.x.y.m() stay unresolved. Native provenance stays EXTRACTED. Binds are extract-time only, never FILE attributes. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ unprefixed field lookup."
origin.session = "default"
[work."WORK-phase-10-of-scc-x-ripwire-lessons-go-one-hop-receiver-field-type-narrow"]
title = "Phase 10 of SCC x Ripwire lessons: Go one-hop receiver-field type narrow\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 10 of SCC x Ripwire lessons: Go one-hop receiver-field type narrowing. Unique Go struct field types pin s.field.method() when s is a method receiver whose type is the enclosing struct. Extract TypeBinds from struct field declarations (owned *Order) and method receivers (func (s *Svc) Run). Resolver: FieldOfVariable with exactly three path segments pins to Type.m only when the unique type of the root variable equals the enclosing type and the field type is unique and that method exists. Two types tombstone. Longer chains stay unresolved. Do not pin import chains like db.users.Find. Native provenance stays EXTRACTED. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ unprefixed field lookup."
origin.session = "default"
[work."WORK-phase-11-of-scc-x-ripwire-lessons-rust-one-hop-self-field-type-narrowin"]
title = "Phase 11 of SCC x Ripwire lessons: Rust one-hop self-field type narrowin\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 11 of SCC x Ripwire lessons: Rust one-hop self-field type narrowing. Unique Rust struct field types pin self.field.method() the same way Python already pins self.x.m(). Extract TypeBinds from struct field declarations (owned: Order, owned: Box<Order>, owned: &Order). Resolver already pins FieldOfSelf with exactly three path segments; the extractor must emit class-scoped binds. Two types tombstone. Longer chains stay unresolved. Native provenance stays EXTRACTED. Binds extract-time only, never FILE attributes. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges."
origin.session = "default"
[work."WORK-phase-12-of-scc-x-ripwire-lessons-java-unprefixed-field-as-receiver-typ"]
title = "Phase 12 of SCC x Ripwire lessons: Java unprefixed field-as-receiver typ\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 12 of SCC x Ripwire lessons: Java unprefixed field-as-receiver type narrowing (Ripwire Rule 2b analogue). Inside a Java method, repo.save() (NamedVariable, exactly two path segments, no this. prefix) pins to the unique field type of the enclosing class when that method exists. A parameter or local of the same name shadows the field and vetoes the narrow even if tombstoned. Two field types tombstone. Longer chains stay unresolved. EXTRACTED provenance. Binds extract-time only. Also P26: Store::open refuses truncated or corrupt scc.db instead of fabricating an empty index. CLI context structural --files with a stale scc:// handle prints HANDLE REFUSED and does not guess a target. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors."
origin.session = "default"
[work."WORK-phase-13-of-scc-x-ripwire-lessons-1-go-extract-time-receiver-field-as"]
title = "Phase 13 of SCC x Ripwire lessons: (1) Go extract-time receiver-field as\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 13 of SCC x Ripwire lessons: (1) Go extract-time receiver-field assignment tombstone: s.owned = &Invoice{} adds a class-scoped field type bind so a conflicting declared type Order plus assignment Invoice tombstones and s.owned.Process() stays unresolved; same-type assignment still pins. (2) Task Context stamps scc:// content handles on a compact FETCH section so agents can lazy-fetch exact source; stale handles must not be guessed. (3) scc bench loop --explore pack-consumer prefers FETCH handles and skips refused stale handles instead of reading the path. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-14-of-scc-x-ripwire-lessons-rust-extract-time-self-field-assignme"]
title = "Phase 14 of SCC x Ripwire lessons: Rust extract-time self-field assignme\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 14 of SCC x Ripwire lessons: Rust extract-time self-field assignment tombstone. self.owned = Invoice {} (one-hop self field) adds a class-scoped field type bind so a conflicting declared type Order plus assignment Invoice tombstones and self.owned.process() stays unresolved; same-type assignment still pins. EXTRACTED provenance. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-15-of-scc-x-ripwire-lessons-go-and-rust-extract-time-local-and-pa"]
title = "Phase 15 of SCC x Ripwire lessons: Go and Rust extract-time local and pa\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 15 of SCC x Ripwire lessons: Go and Rust extract-time local and parameter type narrowing (Ripwire Rule 2 analogue). Unique x := &Order{} / let x = Order {} / typed params pin x.Process() / x.process() to Order when that method exists. Two types tombstone. Conflicting later assignment is tombstone fuel. Same-type assignment still pins. Longer chains stay unresolved. EXTRACTED provenance. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-16-of-scc-x-ripwire-lessons-extract-time-type-assertion-conversi"]
title = "Phase 16 of SCC x Ripwire lessons: extract-time type assertion, conversi\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 16 of SCC x Ripwire lessons: extract-time type assertion, conversion, and cast as Rule 2 fuel. Unique Go v.(*Order) / Order(v) (simple type_identifier only; reject generic_type because Ripwire showed fs[i](3) collides), unique Rust v as Order, and unique TypeScript v as Order / <Order>v pin x.Process()/x.process() when that method exists. Two types tombstone. Longer chains stay unresolved. Opaque factory calls still do not mint a bind. EXTRACTED provenance. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-17-of-scc-x-ripwire-lessons-python-extract-time-identifier-rhs-co"]
title = "Phase 17 of SCC x Ripwire lessons: Python extract-time identifier RHS co\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 17 of SCC x Ripwire lessons: Python extract-time identifier RHS copy bind for Rule 2. Unique x = y when y is uniquely typed (constructor, annotation, or param) pins x.m() to that type when the method exists. Two types tombstone. Conflicting later assignment is tombstone fuel. Longer chains stay unresolved. EXTRACTED provenance. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-18-of-scc-x-ripwire-lessons-score-tests-to-run-in-the-agent-loop"]
title = "Phase 18 of SCC x Ripwire lessons: score tests_to_run in the agent loop \u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 18 of SCC x Ripwire lessons: score tests_to_run in the agent loop against gold tests. Locator and explore arms parse SCC TESTS rows (name + file + reason) and Ripwire pack-task <test p=...> rows and report tests_localization vs tasks.json ground_truth.tests. Baseline has no tests_to_run list and scores 0 when gold tests exist. Empty gold tests are omitted from the clustered mean, not scored as 1.0. Do not change production ranking, MCP tool count, context levels, or packer quotas. This is still a deterministic pack-consumer, not an LLM/SWE-bench repair claim. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-19-of-scc-x-ripwire-lessons-java-extract-time-cast-as-rule-2-fuel"]
title = "Phase 19 of SCC x Ripwire lessons: Java extract-time cast as Rule 2 fuel\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 19 of SCC x Ripwire lessons: Java extract-time cast as Rule 2 fuel. Unique (Order)v pins x.process() when that method exists for var x = (Order)v and later x = (Order)v assignment. Two types tombstone. Array and generic casts do not mint a bind. Longer chains stay unresolved. EXTRACTED provenance. Binds extract-time only. Do not change production ranking, MCP tool count, context levels, or REQ-resolution-honesty-gauges. Do not add C++ extractors or symbol-addressed edits."
origin.session = "default"
[work."WORK-phase-20-of-scc-x-ripwire-lessons-raise-tests-to-run-recall-for-type-scr"]
title = "Phase 20 of SCC x Ripwire lessons: raise tests_to_run recall for TypeScr\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 20 of SCC x Ripwire lessons: raise tests_to_run recall for TypeScript it() titles. Score gold kebab-case names against SCC TESTS titles by ident-token sequence (expands-department-street-names matches expands department street names) without matching Ripwire file-only rows to function names. Fix queue-worker-ts test imports so relative paths resolve (./text and ../geo/resolver). Do not change production ranking, MCP tool count, context levels, or packer quotas. Still a deterministic pack-consumer, not an LLM/SWE-bench repair claim."
origin.session = "default"
[work."WORK-phase-21-of-scc-x-ripwire-lessons-absorb-rule-2c-class-name-receiver-p"]
title = "Phase 21 of SCC x Ripwire lessons: absorb Rule 2c class-name receiver. P\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 21 of SCC x Ripwire lessons: absorb Rule 2c class-name receiver. Pin Cls.m() to the unique in-repo Class/Struct/Interface/Type named Cls that uniquely defines m, after typed local/param binds (Rule 2). Any local or untyped param of that name vetoes the class-name pin. Two same-named class-like defs that both define m stay unresolved (no spray). Do not pin modules. StaticType consults binds; TypeQualified :: stays unchanged. Python and TypeScript untyped parameters are extract-time shadow binds with empty type_name so the veto is visible. Keep extract-time-only binds, four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-22-of-scc-x-ripwire-lessons-absorb-rule-2c-cha-method-on-type-or-ba"]
title = "Phase 22 of SCC x Ripwire lessons: absorb Rule 2c CHA / methodOnTypeOrBa\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 22 of SCC x Ripwire lessons: absorb Rule 2c CHA / methodOnTypeOrBases. When Cls.m() (StaticType) or a unique typed receiver of type Cls does not define m, walk direct bases breadth-first (cap 16 visited names). Pin only when the unique in-repo class-like named Cls has heritage and the shallowest level has exactly one hitting base that uniquely defines m (IERS_B.open() \u2192 IERS.open). Two hitting bases at one level stay unresolved. Two same-named class-likes refuse heritage merge. Persist simple-ident class_bases on CLASS entity attributes (not FILE) so incremental\u2261cold. Python/Java/TS extract heritage. Do not pin modules. No Rule 1 self/super walk yet. Keep extract-time-only type binds, four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-23-of-scc-x-ripwire-lessons-absorb-rule-1-self-this-super-base-wa"]
title = "Phase 23 of SCC x Ripwire lessons: absorb Rule 1 self/this/super base wa\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 23 of SCC x Ripwire lessons: absorb Rule 1 self/this/super base walk. When self.m() or this.m() is not a sibling of the enclosing class, pin via unique class-like plus CHA (IERS_B.run self.open \u2192 IERS.open). Own-class sibling still wins. super.m() and Python super().m() never pin the enclosing class; they walk bases only (skipSelf). Two hitting bases at one level stay unresolved. Classify super() as Super. Do not pin field chains. No C++ bare-name Rule 1. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-24-of-scc-x-ripwire-lessons-absorb-rust-impl-trait-for-t-as-cha-h"]
title = "Phase 24 of SCC x Ripwire lessons: absorb Rust impl Trait for T as CHA h\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 24 of SCC x Ripwire lessons: absorb Rust impl Trait for T as CHA heritage. Ripwire rustImplVisitNode emits inherit Extends (derived T, base Trait). Extract simple-ident trait bases onto T, persist on CLASS/INTERFACE entity attributes like other heritage, name trait methods Trait.m so unique-class-like CHA can pin. Unique typed x: T and Cls.m() pin T.m or the unique hitting trait default/required method. Two traits defining the same method at one level stay unresolved. Inherent impl methods still win over trait CHA. No Go embedding (Ripwire captureBases does not include Go). No C++ extractors. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-25-of-scc-x-ripwire-lessons-absorb-extract-time-function-alias-bi"]
title = "Phase 25 of SCC x Ripwire lessons: absorb extract-time function-alias bi\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 25 of SCC x Ripwire lessons: absorb extract-time function-alias binds for bare f(). Ripwire fnPtrBindingTarget is C/C++/ObjC only and fires before Rule 1: unique var=function ident pins f() to that function, never falls back to a same-named global, two targets/lambda/clobber tombstone. SCC analog for Python/TypeScript/Go/Rust: unique extract-time f = helper / const f = helper / f := helper / let f = helper pins RecvKind::None f() to the unique in-repo Function (local, then import, then unique-across-repo). Lambda/arrow/closure is an empty-target bind that blocks the name ladder. A later non-function assignment clobbers only when a fn bind already exists. Type-copy ident RHS is not a fn bind but clobbers a prior fn bind. File-scope empty-scope binds apply when the caller has no local bind; local vs file disagreement refuses. Do not pin Class constructor aliases. No C++ extractors. No Go embedding. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-fix-pr-review-comments-without-collapsing-scc-type-script-non-null-asser"]
title = "Fix PR review comments without collapsing SCC: TypeScript non-null asser\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Fix PR review comments without collapsing SCC: TypeScript non-null assertions stay Call not Macro and strip per-segment bangs; paths_depending_on only IMPORTS and CALLS; sandbox task-context file reads; hash empty files in handles; protobuf service newline before brace; observed upgrades count only new inserts; Ripwire nonzero exit is a failed pack; min-delta 0.0 still gates; hash-sweep indexes newly created files; honesty gauges mark IndexSearch as unsupported not parsed; cheap store probe instead of PRAGMA quick_check on every open; tests_to_run state includes persistent stores; retrieval gold keys preserve entity rank; ranking arms NoTaskPpr and NoGlobalPpr use staged surface ablations; write-matrix infrastructure failures keep an error key. Keep production ranker default, 10 MCP tools, and four context levels. Also pin TypeScript function-valued consts through Const plus signature without retagging arrows as Function."
origin.session = "default"
[work."WORK-phase-26-of-scc-x-ripwire-lessons-absorb-rule-3-import-include-file-nar"]
title = "Phase 26 of SCC x Ripwire lessons: absorb Rule 3 import/include-file nar\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 26 of SCC x Ripwire lessons: absorb Rule 3 import/include-file narrowing. Ripwire rule3IncludeFile is language-agnostic on the path-precise file-to-file import/include graph: it fires only when the caller has at least one resolved import, no same-name candidate lives in the caller file (same-file is the local ladder), and exactly one imported file holds a same-name callable. Zero or two-plus imported defining files stay unresolved. Never invent a target. Never basename-guess. SCC analog uses existing IMPORTS and resolve_import Internal files (not C++ extractors, not Go go.mod, not Java namespaces, not transitive C include closure). Bare helper() with two defs: unique imported defining file pins; both imported stay unresolved; neither imported stays unresolved; same-file def wins and Rule 3 must not override. Also pin when the unique imported file is the only in-repo def (SCC has no unique-across-repo bare-name ladder). Function-alias unique_function_id uses the same Rule 3 after local and named-import, before unique-across-repo. Path-precise: from pkg.a import foo must pin pkg/a.py not other/a.py. No C++ extractors. No fifth context level. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-27-of-scc-x-ripwire-lessons-absorb-rust-step-a-path-precise-impor"]
title = "Phase 27 of SCC x Ripwire lessons: absorb Rust Step-A path-precise impor\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 27 of SCC x Ripwire lessons: absorb Rust Step-A path-precise import resolution (resolveRustImport). Unique-or-degrade: use crate::a::b / super:: / self:: and body-less mod x; resolve to exactly one .rs or /mod.rs file or contribute nothing. Never basename-guess. Never pick when both a.rs and a/mod.rs exist. std:: and bare external crates stay External. crate:: without a locatable crate root (src/lib.rs or src/main.rs, first in sorted file list) degrades to Unresolved. Brace groups degrade. Gate on .rs callers so Python/TS/Go do not steal .rs files. No go.mod, no C++ extractors, no Java namespaces, no transitive include closure. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-28-of-scc-x-ripwire-lessons-absorb-python-and-type-script-step-a-p"]
title = "Phase 28 of SCC x Ripwire lessons: absorb Python and TypeScript Step-A p\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 28 of SCC x Ripwire lessons: absorb Python and TypeScript Step-A path-precise import resolution (resolvePythonImport / resolveTsImport). Unique-or-degrade: Python import a / import pkg.mod / from pkg.mod import Z / from .rel import Z resolve to exactly one .py or /__init__.py or contribute nothing. Never basename-guess. Never pick when both pkg.py and pkg/__init__.py exist. Relative leading-dot imports probe includer-dir only. Absolute imports probe file-relative, repo-root, and SCC unique source-root fallback (src/svc/lib/app/services/service/packages) as one unique-or-degrade set: only src/util.py still pins, util.py and src/util.py degrade. Zero absolute hits stay External; two-plus in-repo hits stay Unresolved (not External, not a guess). TypeScript relative ./x and ../a/b probe exact then a fixed extension list then index files relative-to-includer; unique hit pins, 0 or two-plus degrade to Unresolved. Bare TS specifiers (react, lodash) stay External and must not match in-repo files by basename. Language-gate on caller extension (.py vs .ts/.tsx/.js/.jsx/.mjs/.cjs) so Python does not steal TS files and TS does not steal Python files. No tsconfig multi-root aliases, no go.mod, no C++ extractors, no Java namespaces, no transitive include closure. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-29-of-scc-x-ripwire-lessons-absorb-remaining-language-gated-step"]
title = "Phase 29 of SCC x Ripwire lessons: absorb remaining language-gated Step-\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 29 of SCC x Ripwire lessons: absorb remaining language-gated Step-A honesty for Go and Java without cargo-culting go.mod or Java namespaces. Gate on caller extension so Go/Java never steal Python/TS files via the generic first-match resolver (import fmt must stay External even if fmt.py exists). Java type imports (import com.foo.Bar) unique-or-degrade to exactly one .java file by dots-to-slashes plus .java, including unique SCC Java source-root fallback src/main/java and src/test/java; star imports (com.foo.*) contribute no file (namespace, not a type); zero hits stay External; two-plus in-repo hits stay Unresolved. Go import paths unique-or-degrade to a package: exactly one of {path}.go or the non-test .go files directly in {path}/, plus unique source-root fallback; {path}.go and {path}/ both present degrade; two package dirs degrade; a unique package directory with several .go files expands to one Internal import per file so Rule 3 sees the whole package (not a guessed representative file); *_test.go is not a production package file; no go.mod parsing, no module-path suffix guessing, no C++ extractors. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-phase-30-of-scc-x-ripwire-lessons-absorb-c-and-c-extractors-with-path"]
title = "Phase 30 of SCC x Ripwire lessons: absorb C and C++ extractors with path\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Phase 30 of SCC x Ripwire lessons: absorb C and C++ extractors with path-precise quote-include Step-A. Promote C and C++ from IndexSearch to SemanticDeep with fixtures: tree-sitter extract functions, C++ class methods, calls, and #include. Quote includes (#include \"foo.h\") resolve by lexical join with the includer directory only \u2014 exact file hit pins, miss is Unresolved, never basename-guess other/foo.h. Angle includes (#include <stdio.h>) stay External even if a same-named header exists in-repo. Direct includes only \u2014 not Ripwire transitive include closure, not compile_commands -I, not C++ CHA, not fn-pointer address-of/escape, not ObjC, not .scm rewrite. Gate on C/C++ extensions so they never steal .py/.ts. Keep four context levels, 10 MCP tools, and the production ranker."
origin.session = "default"
[work."WORK-merge-origin-main-into-the-ripwire-extraction-pr-branch-and-resolve-conf"]
title = "Merge origin/main into the Ripwire extraction PR branch and resolve conf\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Merge origin/main into the Ripwire extraction PR branch and resolve conflicts. Keep every Ripwire WORK entry and main's existing merge-conflict work item. Do not drop Phase 30 C/C++ extractor work."
origin.session = "default"
[work."WORK-resolve-merge-conflicts-with-origin-main-and-address-remaining-code-rabbi"]
title = "Resolve merge conflicts with origin/main and address remaining CodeRabbi\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Resolve merge conflicts with origin/main and address remaining CodeRabbit/Codex review comments on PR #2 for P0 OMP integration correctness and writable-benchmark scientific validity. Keep frozen SCC architecture. Pair AGENTS.md markers without spanning a second open; keep scc_commit; fail-closed TraceLayer; exclude infra/undefined task_success from aggregates; executable evaluator assertions."
origin.session = "default"
[work."WORK-stop-re-running-the-golden-integration-suite-in-every-scc-cli-test-binar"]
title = "Stop re-running the golden integration suite in every scc-cli test binar\u2026"
status = "active"
origin.source = "new_feature"
origin.intent = "Stop re-running the golden integration suite in every scc-cli test binary. Cargo treats mod golden as compiling golden.rs into each binary, so all 14 tests run about 30 times and starve CI. Split helpers into tests/common/mod.rs with no tests; keep golden.rs as the only binary that runs those tests."
origin.session = "default"
